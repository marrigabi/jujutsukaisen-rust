<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <h1>{{ title }}</h1>
    <form action="/update-user/{{ user.id }}" method="post">
        <label>Nome: <input type="text" name="nome" value="{{ user.nome }}" required></label><br>
        <label>Sobrenome: <input type="text" name="sobrenome" value="{{ user.sobrenome }}" required></label><br>
        <label>CPF: <input type="text" name="cpf" value="{{ user.cpf }}" required></label><br>
        <label>Email: <input type="email" name="email" value="{{ user.email }}" required></label><br>
        <label>Telefone: <input type="text" name="telefone" value="{{ user.telefone }}" required></label><br>
        <label>Login: <input type="text" name="login" value="{{ user.login }}" required></label><br>
        <label>Senha: <input type="password" name="senha" required></label><br>
        <button type="submit">Salvar Alterações</button>
    </form>
</body>
</html>
